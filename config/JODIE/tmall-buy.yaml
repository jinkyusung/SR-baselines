wandb:
  entity: jinkyusung-snu
  project: SR-baselines
  run_id: None

dataset:
  name: tmall-buy
  k_core: 10
  max_len: 50
  batch_size: 512

model:
  name: JODIE
  emb_dim: 128

trainer:
  name: JODIETrainer
  epochs: 50
  lr: 0.001
  weight_decay: 0.0005
